cluster:
  id: test-cluster
  role: worker
  endpoint: "http://main:8787"
  token: "test-token-2026"

node:
  id: node-worker
  name: worker-node
  capabilities:
    - coding
    - gpu
    - browser

server:
  bind: "0.0.0.0"
  port: 8787

lease:
  ttl: 30s
  scan_rate: 5s

watchdog:
  check_interval: 3s
  degraded_after: 10s
  offline_after: 20s
